<UserControl x:Class="PrismDatabaseApp.Views.DryingProcessView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True" Cursor="">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- URL 입력 -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="400" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Navigate" Command="{Binding NavigateCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <!-- WebBrowser 컨트롤 -->
        <WebBrowser x:Name="Browser" Grid.Row="1" />
    </Grid>
</UserControl>
