<UserControl x:Class="PrismDatabaseApp.Views.AlarmBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrismDatabaseApp.Views"
             mc:Ignorable="d"  Background="#1A1A1A"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- 알림 -->
        <Border Background="#1A1A1A" CornerRadius="5" Padding="10">
            <StackPanel>
                <!-- 제목 -->
                <TextBlock Text="Notifications" FontSize="20" Foreground="White" Margin="10" />

                <!-- 알람 리스트 -->
                <ListView ItemsSource="{Binding Alarms}" Margin="10" Background="#2D2D2D">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="5" Background="#2D2D2D">
                                <!-- Column 정의 -->
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <!-- 메시지와 타임스탬프 -->
                                    <ColumnDefinition Width="Auto" />
                                    <!-- 상세 버튼 -->
                                    <ColumnDefinition Width="Auto" />
                                    <!-- 삭제 버튼 -->
                                </Grid.ColumnDefinitions>

                                <!-- 알람 메시지와 타임스탬프 -->
                                <StackPanel Orientation="Vertical" Grid.Column="0" Margin="5">
                                    <TextBlock Text="{Binding Message}" VerticalAlignment="Center" Foreground="White" FontSize="16" />
                                    <TextBlock Text="{Binding Timestamp}" VerticalAlignment="Center" Foreground="Gray" FontSize="12" />
                                </StackPanel>

                                <!-- 상세 버튼 -->
                                <Button Content="상세"
                                Command="{Binding DataContext.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding}"
                                Margin="5" Grid.Column="1" Width="60" Height="30" />

                                <!-- 삭제 버튼 -->
                                <Button Content="삭제"
                                Command="{Binding DataContext.DeleteAlarmCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding}"
                                Margin="5" Grid.Column="2" Width="60" Height="30" Background="#FF3B30" Foreground="White" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
