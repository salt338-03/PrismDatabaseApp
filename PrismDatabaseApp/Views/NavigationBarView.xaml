<UserControl x:Class="PrismDatabaseApp.Views.NavigationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity">
    <StackPanel>
        <TextBlock Text="Acme Smart Factory" Foreground="White" FontSize="20" Margin="10" />
        <Button Content="Slurry Supply Process" 
                Command="{Binding NavigateCommand}" 
                CommandParameter="SlurrySupplyProcessView" 
                 />
        <Button Content="Coating Process" 
                Command="{Binding NavigateCommand}" 
                CommandParameter="CoatingProcessView" 
                 />
        <Button Content="Drying Process" 
                Command="{Binding NavigateCommand}" 
                CommandParameter="DryingProcessView" 
                 />
    </StackPanel>
</UserControl>
