<UserControl x:Class="PrismDatabaseApp.Views.SlurrySupplyProcessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#1A1A1A">
    <!-- ScrollViewer로 전체 콘텐츠 감싸기 -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20">
            <!-- Grid Row 정의 -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- 상단 제목 및 진행률 -->
                <RowDefinition Height="200" />
                <!-- 그래프 -->
                <RowDefinition Height="Auto" />
                <!-- 주요 메트릭 -->
                <RowDefinition Height="*" />
                <!-- 알림 리스트 -->
            </Grid.RowDefinitions>

            <!-- 상단 섹션: 제목 및 진행률 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Slurry Supply Process" FontSize="24" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="Running since 9:00 AM" FontSize="14" Foreground="Gray" />
                <ProgressBar Value="{Binding Progress}" Maximum="100" Height="20" Margin="0,10,0,0" Foreground="#00A4FF" Background="#2D2D2D" />
                <TextBlock Text="{Binding EstimatedTime}" FontSize="14" Foreground="Gray" />
            </StackPanel>

            <!-- 데이터 그래프 -->
            <Border Grid.Row="1" Background="#2D2D2D" CornerRadius="5" Margin="0,0,0,20">
                <TextBlock Text="(Graph Placeholder - Implement Live Charts here)" 
                           Foreground="Gray" 
                           VerticalAlignment="Center" 
                           HorizontalAlignment="Center" />
            </Border>

            <!-- 주요 메트릭 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,20">
                <Border Margin="10" Padding="20" Background="#333" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Slurry Supply Speed" Foreground="White" FontSize="14" />
                        <TextBlock Text="{Binding SlurrySupplySpeed}" Foreground="White" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Margin="10" Padding="20" Background="#333" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Slurry Supply Volume" Foreground="White" FontSize="14" />
                        <TextBlock Text="{Binding SlurrySupplyVolume}" Foreground="White" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Margin="10" Padding="20" Background="#333" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Slurry Supply Pressure" Foreground="White" FontSize="14" />
                        <TextBlock Text="{Binding SlurrySupplyPressure}" Foreground="White" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Margin="10" Padding="20" Background="#333" CornerRadius="5">
                    <StackPanel>
                        <TextBlock Text="Slurry Supply Temperature" Foreground="White" FontSize="14" />
                        <TextBlock Text="{Binding SlurrySupplyTemperature}" Foreground="White" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 알림 -->
            <Border Grid.Row="3" Background="#2D2D2D" CornerRadius="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Notifications" FontSize="20" Foreground="White" Margin="10" />
                    <ListBox ItemsSource="{Binding Notifications}" Background="#1A1A1A" Foreground="White" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
